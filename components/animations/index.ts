// /components/animations — GSAP Hooks & Lenis Smooth Scroll
//
// RULE: ALL vector and layout movement must be routed through hooks in this folder.
// Standard CSS @keyframes, transition, and animation are STRICTLY FORBIDDEN for
// any layout or interactive element animation.
//
// Intended contents:
//   - useLenis.ts           — Lenis smooth scroll singleton hook
//   - useGSAPTimeline.ts    — Base GSAP timeline factory with defaults
//   - useScrollTrigger.ts   — GSAP ScrollTrigger utility hook
//   - useFadeIn.ts          — Standard fade-in entrance animation
//   - useStaggerReveal.ts   — Staggered text/element reveal hook
//   - useParallax.ts        — GSAP-driven parallax depth effect
//   - useMagneticHover.ts   — Magnetic cursor hover effect for CTAs
//   - usePageTransition.ts  — Route transition animation controller

export {}; // barrel export — populate as hooks are added
