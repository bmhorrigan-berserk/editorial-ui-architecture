// /components/layout — Structural Layout Components
//
// RULE: Claude must NEVER generate a new layout component from scratch if an
// equivalent already exists here. Always import and modify an existing asset.
//
// Intended contents:
//   - Nav.tsx            — Primary navigation bar with logo and links
//   - HeroSection.tsx    — Full-viewport hero with headline and CTA
//   - FooterGrid.tsx     — Multi-column footer with editorial grid
//   - PageWrapper.tsx    — Root page layout with global padding/constraints
//   - SectionDivider.tsx — Whitespace section separator
//   - GridSystem.tsx     — 12-column editorial grid container

export {}; // barrel export — populate as components are added
